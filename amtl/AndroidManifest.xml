<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.intel.amtl"
    android:versionCode="1"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system" >

    <uses-sdk android:minSdkVersion="10" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ModemtraceserverActivity" android:label="Modem Trace Server (mts)"/>
        <activity android:name=".Configure_trace_modemActivity" android:label="mts : configure trace modem"/>
        <activity android:name=".Activate_trace_modemActivity" android:label="mts : activate trace modem"/>
        <activity android:name=".UsbswitchActivity" android:label="Usbswitch"/>
        <activity android:name=".Toggle_pinActivity" android:label="Toggle pin on1"/>
        <activity android:name=".Trace_in_coredumpActivity" android:label="Traces in coredump"/>
    </application>

</manifest>